# ─── Server ───
# PORT=3001

# ─── Admin ───
ADMIN_PASSWORD=semiconwar2026

# ─── Market Data Providers ───
# TWELVE_DATA_API_KEY=your_key_here      # Free: twelvedata.com (8 req/min)
# DART_API_KEY=your_dart_api_key_here    # Free: opendart.fss.or.kr
# yahoo-finance2 does not require an API key (installed as npm optional dep)

# ─── Feature Flags (env var overrides) ───
# USE_MOCK_MARKET_DATA=true              # true: mock data (dev), false: live providers
# ENABLE_LIVE_MARKET_BUFFS=false         # true: stock % → in-game team buff
# ENABLE_NEWS_EVENTS=false               # true: DART news + admin events
# ENABLE_LIVE_MARKET_PANEL=true          # true: show stock panel in HUD

# ─── Market Refresh ───
# MARKET_REFRESH_INTERVAL_SEC=300        # Polling interval in seconds (default 300 = 5min)
